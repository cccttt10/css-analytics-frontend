import Layout from '../layout/BaseLayout/';
import UrlForm from '../components/UrlForm';

export default Layout;

<UrlForm showLabel={true} />

This tool will help you:

1. Visualize important maintainbility metrics about your stylesheets
   so that you can make your stylesheets more maintainble and scalable.

2. Visualize key design elements in your stylesheets so that front-end
   developers and UI designers can communicate about design decisions without
   involving any code.

---

We also offer an API service that can be embedded in other applications. [Learn more](/api)
